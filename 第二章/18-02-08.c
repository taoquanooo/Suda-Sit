#include <stdio.h>
//从键盘依次输入平时、期中和期末成绩（均为整数），输出总评成绩（保留2位小数）。平时占15%，期中占25%，期末占60%。
int main()
{
	int d1,d2,d3;
	scanf("%d%d%d",&d1,&d2,&d3);
	printf("%0.2f",d1*0.15+d2*0.25+d3*0.6);
	return 0;
}
